možná se v css zbavit toho měření podle scrrenu...např. když se errory rozjedou moc dolů, tak už to tam nedodáhne - šlo by předělat na 100%

otestovat addVector
    existující bod do existujícího
        vektor má stejné body - spadne
        vektor už existuje - spadne
        vektor ještě neexisuje - vytvoří se
    nový bod do existujícího
        vektor má stejné body - spadne
        vektor už existuje - spadne
        vektor ještě neexisuje - vytvoří se vektor i bod
        nový bod je už existující bod - vektor se vytvoří a nepřidá se nový bod
    existující bod do nového
        vektor má stejné body - spadne
        vektor už existuje - spadne
        vektor ještě neexisuje a bod taky ne - vytvoří se vektor i bod
        nový bod je už existující bod - vektor se vytvoří a nepřidá se nový bod
    nový bod do nového
        vektor má stejné body - spadne
        vektor už existuje - spadne
        oba body jsou nové - přidají se jak body tak vektor
        oba body už existují - přidá se jenom vektor

udělat chybovou hlášku tak, aby jich tam mohlo vyskočit několik
    když bude něco stejného typu, tak se to jenom přidá, když bude různého, tak se vyrobí nový alert pod ten původní

pokusit se fixnout ten problém s animací - moc ale nevím jak - možná když to bude řídit jen jeden timeout? nevím. Nebo nějak resetovat ten timeout za kdy to má zmizet
    možná si ten timeout uložit jako stav

uprava dat. modelu
    points budou mít set layers - výpočty budou probíhat pouze v rámci jedné layer, ale bude snadné layers přidávat
    vector nebude mít layers - jeho layers budou sjednocením layers jeho bodů
    bude potřeba upravit i v tříde Model, aby se s tím počítalo, když se naměří stejný drawable jen v jiné layer - k existujícímu drawable se pak přidá layer

udělat TODOčka v kódu - udělat errory pro přidávání již existujících nebo jinak chybných elementů

Udělat layers tab (CRUD operace s layers)
    Při odinstalování layer tam musí být warning, když pod sebou layer má nějaké objekty

začít s measure
    nějakým způsobem umět vykreslit uzavřené 2D a 3D objekty - existuje v teorii lineárních modelů nějaký algoritmus na identifikaci?
        2D - návrh algoritmu:
            1. najít všechny kružnice v neorientovaném grafu
            2. zjistit jestli leží kružnice v jedné rovině pomocí normálového vektoru
            To bude relativně náročný algoritmus, možná to půjde optimalizovat tak, že se ten normálový vektor stanový už při nalezení prvních dvou vektorů
            
            při každém přidání vektoru se zjistí jestli se neutvořila nová 2D plocha
                najdou se všechny vektory, po kterých se dá jít z nového vektoru
                pokud vzniknout nějaké kružnice, tak se zkontroluje, že jsou v jedné rovině
                přidá se nová 2D plocha pokud ještě neexistuje
                pokud se větší 2D plocha rozdělila na menší, tak se nechá větší a vytvoří se k ní ty menší - necháváme všechny, aby se s nimi dalo potom počítat
            
            při každém přidání/oddělání 2D plochy se musí řešit 3D plochy
            
        3D - minimálníé objekt je jehlan
            část 3D objektu musí sdílet právě 2 body - vytvoří tím roh dvou stěn
